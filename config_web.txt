(define default_port 8080);
(define max_workers 4);

server_name := 'my-web-server';
port := ![default_port + 1000];
log_level := 'info';

workers := #( 'worker1' 'worker2' 'worker3' 'worker4' );

database_config := begin
  host := 'localhost';
  port := 5432;
  name := 'app_db';
  pool_size := 10;
end;

features := begin
  caching := true;
  compression := false;
  ssl := begin
    enabled := true;
    cert_file := '/path/to/cert.pem';
  end;
end;